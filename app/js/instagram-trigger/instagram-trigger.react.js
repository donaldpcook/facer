'use strict';

var React = require('react');

var InstagramTrigger = React.createClass({
  render: function() {
    return (
      <button>Authorize Instagram</button>
    )
  }
});

module.exports = InstagramTrigger;
